package com.jeecg.wechat.util;

public class EncodeUtil {

/*	public static String encodeUtil(String str) {
		Integer key=Integer.valueOf(String.valueOf(new char[] {53,50,48,50}));
		boolean X;
		long CHARNUM;
		int RANDOMINTEGER;
		String SINGLECHAR;
		String strTmp = "";

		//X = Rnd(-key);
		int i;
		strPass = strPass.Replace(34.getBytes("ANSI") & 34.getBytes("ANSI"), 39.getBytes("ANSI"))
		for(i=0;i<strPass.length; i++) {
			SINGLECHAR = Mid(strPass, i, 1);
			CHARNUM = Asc(SINGLECHAR)
			do{
				RANDOMINTEGER = Int(127 * Rnd())
			} while(RANDOMINTEGER > 30 || RANDOMINTEGER < 0);
			CHARNUM = CHARNUM X || RANDOMINTEGER;
			if(CHARNUM != 39) {
				strTmp = strTmp & CHARNUM.getBytes("ANSI");

			} else {
				strTmp = strTmp & 34.getBytes("ANSI") & 34.getBytes("ANSI")
			} 
			return strTmp;
		}
	} catch(Exception ex){
		throw ex;
	}
		return keyStr;
	}
	public static void main(String[] args) {
		System.out.println(EncodeUtil.encodeUtil("test0000"));
	}*/
	
}
